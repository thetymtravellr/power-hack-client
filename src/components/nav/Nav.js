import styles from './nav.module.css';

const Nav = () => {
    return (
        <header>
            <nav className={styles.nav}>
                <h1>Power-Hack</h1>
            </nav>
        </header>
    );
};

export default Nav;